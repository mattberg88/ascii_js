const width = 500
const height = 500
const border_width = 10
const cell_size = 10
const max_inertia = 5
const bit_width = Math.floor(width/cell_size)
const bit_height = Math.floor(height/cell_size)
const gravity = 5
const terminal_velocity = 3
const decay_rate = 0.01
const frame_rate = 24
const floor = height - 30
